# Optional local overrides for scripts/bootstrap-cdc.sh.
# Copy this file to .env only when the defaults do not match your local stack.

ERP_DB_ROOT_USER=root
ERP_DB_ROOT_PASSWORD=
CDC_DB_USER=debezium
CDC_DB_PASSWORD=
DEBEZIUM_SERVER_ID=184054
TOPIC_PREFIX=erpnext
TARGET_TABLES=tabEmployee,tabAttendance
ERP_DB_NAME=
KAFKA_CONNECT_URL=http://localhost:8083
