# do these command on both frontend and backend container in one line
bench get-app https://github.com/DeITdev/iot_hcm.git && bench --site frontend install-app iot_hcm && bench migrate

# uninstall app
bench --site frontend uninstall-app iot_hcm

# remove from apps folder
bench remove-app iot_hcm

# unistall and remove from apps folder
bench --site frontend uninstall-app iot_hcm && bench remove-app iot_hcm