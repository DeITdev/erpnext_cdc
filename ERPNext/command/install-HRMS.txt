# do these command on both frontend and backend container in one line
bench get-app hrms && bench --site frontend install-app hrms && bench migrate

# uninstall app
bench --site frontend uninstall-app hrms

# remove from apps folder
bench remove-app hrms

# unistall and remove from apps folder
bench --site frontend uninstall-app hrms && bench remove-app hrms