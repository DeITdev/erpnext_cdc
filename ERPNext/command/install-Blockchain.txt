# do these command on both frontend and backend container in one line
bench get-app https://github.com/DeITdev/blockchain_hcm.git && bench --site frontend install-app blockchain_hcm && bench migrate

# uninstall app
bench --site frontend uninstall-app blockchain_hcm

# remove from apps folder
bench remove-app blockchain_hcm

# unistall and remove from apps folder
bench --site frontend uninstall-app blockchain_hcm && bench remove-app blockchain_hcm